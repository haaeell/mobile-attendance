import { EmptyState } from '@/components/EmptyState';

/**
 * Placeholder. Implementasi scanner barcode dikerjakan pada tahap
 * berikutnya (di luar cakupan fondasi ini).
 */
export default function ScanScreen() {
  return <EmptyState title="Scan" message="Fitur scan barcode akan dikerjakan pada tahap berikutnya." />;
}
