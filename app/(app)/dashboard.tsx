import { EmptyState } from '@/components/EmptyState';

export default function DashboardScreen() {
  return <EmptyState title="Dashboard" message="Konten dashboard akan dikerjakan pada tahap berikutnya." />;
}
