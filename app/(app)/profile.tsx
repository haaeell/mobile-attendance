import { EmptyState } from '@/components/EmptyState';

export default function ProfileScreen() {
  return <EmptyState title="Profil" message="Konten profil akan dikerjakan pada tahap berikutnya." />;
}
